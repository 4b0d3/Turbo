{% set navActive = "returns" %}

{% extends "templates/admin.html.twig" %}

{% block content %}
    <section id="container-admin">

        {{ include('includes/boxMessages.html.twig') }}

        <header id="admin-header">
            <h1>Liste des retours</h1>
            <nav>
            </nav>
        </header>

        <nav id="admin-nav">
            <ul>
                <li><a href="{{ HERE }}" class="active">Retours</a></li>
            </ul>
        </nav>

        <section id="admin-content">
            <table id="table-content">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Id d'user</th>
                        <th>Address</th>
                        <th>Total</th>
                        <th>Produits</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for return in returns %}
                        <tr>
                            <td>{{ return.id }}</td>
                            <td>{{ return.idUser }}</td>
                            <td>{{ return.address }}</td>
                            <td>{{ return.total }}€</td>
                            <td>{{ return.products }}</td>
                            <td>
                                <a href="{{ HERE }}{{ return.id }}/valide/">Valider</a>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>

            <nav id="pagination">

            </nav>
        </section>
    </section>
{% endblock content %}