{% set navActive = "subscriptions" %}

{% extends "templates/admin.html.twig" %}

{% block content %}
    <section id="container-admin">

       {{ include('includes/boxMessages.html.twig') }}

        <header id="admin-header">
            <h1>Supprimer un Abonnement</h1>
        </header>

        {% include "admin/subscriptions/navAdminSubscriptions.html.twig" with {active : "delete"} %}

        <section id="admin-content">
            <form action="{{ HERE }}" method="POST" id="form-subscriptions-add">
                <p>
                    Etes-vous sûr de vouloir supprimer l'abonnement "{{ pr }}" ?
                </p>
                <input type="hidden" name="id" value="{{ subscription.id }}">
                <div class="box-input-submit">
                    <input type="submit" value="supprimer">
                </div>
            </form>
        </section>
    </section>
{% endblock content %}